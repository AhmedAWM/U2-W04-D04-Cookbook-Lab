<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cookbook - Signin</title>

    <!-- Adding style to the page -->
    <%- include('../views/partials/head') %>
  </head>
  <body>
    <!-- Adding header to the page -->
    <%- include('../views/partials/header') %>
    <main>
      <section class="section-hero grid--c-1-1">
        <div class="hero signin-container">
          <form action="/auth/signin" method="POST" class="signin-form">
            <h1 class="title">Welcome to Cookbook!</h1>
            <h2 class="subtitle">Please fill in sign in information</h2>
            <label for="email">Email</label>
            <input
              type="email"
              id="email"
              name="email"
              class="input-field"
              required
            />

            <label for="password">Password</label>
            <input
              type="password"
              id="password"
              name="password"
              class="input-field"
              required
            />

            <div>
              <button class="signin-signup-button">Sign In</button>
            </div>

            <div class="signin-or-signup">
              <p>Not a member? <a href="/auth/signup">Create an account</a></p>
            </div>
          </form>
        </div>
        <div class="hero hero-image-container">
          <img src="/img/person-eating.svg" alt="" class="hero-image" />
        </div>
      </section>
    </main>
  </body>
</html>
